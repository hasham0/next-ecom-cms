type SlidesTs = {
  id: number;
  title: string;
  description: string;
  img: string;
  url: string;
  bg: string;
};
type ImagesTS = {
  id: number;
  url: string;
};
export type { SlidesTs, ImagesTS };
